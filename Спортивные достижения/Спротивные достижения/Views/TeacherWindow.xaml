<Window x:Class="Спротивные_достижения.Views.TeacherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Спротивные_достижения.Views"
        mc:Ignorable="d"
         Title="Личный кабинет преподавателя" 
        Height="1000" 
        Width="1200" 
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" 
                Background="#4CAF50" 
                Height="80"
                CornerRadius="0,0,20,20">
            <Grid Margin="20,0">
                <TextBlock Text="Личный кабинет преподавателя" 
                           Foreground="White" 
                           FontSize="24" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI"/>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center">
                    <TextBlock x:Name="txtUserName" 
                               Foreground="White" 
                               FontSize="16"
                               VerticalAlignment="Center"
                               Margin="0,0,15,0"/>
                    <Button x:Name="btnChangePassword" 
                            Content="Смена пароля" 
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="1"
                            BorderBrush="White"
                            FontSize="13"
                            Padding="10,5"
                            Margin="0,0,15,0"
                            Cursor="Hand"
                            Click="BtnChangePassword_Click"/>
                    <Button x:Name="btnLogout" 
                            Content="Выйти" 
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="1"
                            BorderBrush="White"
                            FontSize="14"
                            Padding="10,5"
                            Cursor="Hand"
                            Click="BtnLogout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Блок информации о группе -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Padding="15"
                Margin="20,15,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="Группа:" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Foreground="#333333"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <TextBlock x:Name="txtGroupName" 
                           Grid.Column="1"
                           Text="Группа не назначена" 
                           FontSize="14"
                           Foreground="#4CAF50"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="txtStudentCount" 
                           Grid.Column="3"
                           Text="Всего: 0" 
                           FontSize="14"
                           Foreground="#666666"
                           VerticalAlignment="Center"
                           />

                <Button x:Name="btnViewAchievements" 
                        Grid.Column="1"
                        Content="Посмотреть достижения студента" 
                        Background="#9C27B0"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="12"
                        Height="32"
                        Cursor="Hand"
                        IsEnabled="False"
                        Click="BtnViewAchievements_Click" Margin="392,0,392,0"/>
            </Grid>
        </Border>

        <!-- Таблица студентов (ListView) -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Padding="0"
                Margin="20,0,20,15">
            <ScrollViewer x:Name="StudentsScrollViewer"
                          VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto"
                          PreviewMouseWheel="StudentsScrollViewer_PreviewMouseWheel">
                <ListView x:Name="lvStudents" 
                          SelectionMode="Single"
                          Background="Transparent"
                          BorderThickness="0"
                          AlternationCount="2"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.PanningMode="Both"
                          SelectionChanged="LvStudents_SelectionChanged">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                            <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="100"/>
                            <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="100"/>
                            <GridViewColumn Header="Отчество" DisplayMemberBinding="{Binding MiddleName}" Width="100"/>
                            <GridViewColumn Header="Логин" DisplayMemberBinding="{Binding Login}" Width="120"/>
                            <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="180"/>
                            <GridViewColumn Header="Телефон" DisplayMemberBinding="{Binding Phone}" Width="110"/>
                            <GridViewColumn Header="Дата зачисления" DisplayMemberBinding="{Binding EnrollmentDate, StringFormat='dd.MM.yyyy'}" Width="100"/>
                        </GridView>
                    </ListView.View>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#F9F9F9"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E0E0E0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </ScrollViewer>
        </Border>

        <!-- Нижняя панель -->
        <Border Grid.Row="3" 
                Background="#E0E0E0" 
                Height="40"
                CornerRadius="20,20,0,0">
            <TextBlock Text="© Система учета спортивных достижений" 
                       Foreground="#666666"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
